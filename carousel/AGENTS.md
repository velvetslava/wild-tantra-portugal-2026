# Instructions for agents working on this carousel

Before editing, read these files completely:

1. `HANDOFF_FOR_SLAVA.md`
2. `workflow/README.md`
3. `workflow/history/DESIGN_RULES.md`
4. `workflow/skills/ideal-carousel/SKILL.md`
5. `workflow/skills/instagram-carousel-ai-first/SKILL.md`

The canonical editable build is `index.html`. The approved Russian copy is in `source/script_ru.md`. Do not paraphrase, shorten, expand, translate, or factually alter approved copy unless the user explicitly asks for that exact editorial change.

Keep every slide at 1080 × 1350 px and keep `.slide` as the export selector. After any approved change, regenerate `png/`, the ZIP, and `contact-sheet.jpg`; then update `workflow/status.yaml` with the date and a concise note.

Preserve the current visual direction: dark powder, restrained pink, olive, paper, Onest, meaningful real photographs, compact editorial typography, and informative composition. Avoid unnecessary oversized headings, empty lower halves, generic UI cards, and table-like layouts.

Historical variants under `workflow/history/` are references only. Do not promote one over `index.html` unless the user explicitly selects it.
